unit branch;

interface

uses classes;

procedure branch(chunk_list: tstringlist; lineNum: integer);

implementation

// close_control_flow()

procedure branch(chunk_list: tstringlist; lineNum: integer);
begin
//	if chunk_list.count > 0 then writeln(chunk_list.valuefromIndex[0] + ' ' + chunk_list.valuefromIndex[1]);

// if-then statements

// simple loops

// foreach loops

// raise exception_name
	// search identifier_list for a matching exception
	// if match found, call it
	// if no match found, print an appropriate exception message

end;

end.

