



def booleanDeclaration(snippet):

	# variableDeclarationOutput = sjExpressions.booleanAssignment(assign_value)
	
	

def scalarDeclaration(snippet):
	
	# if snippet[1] == 'truth':
		# call booleanDeclaration
		
		
	# handle rich truth variables separately

	


# character declarations
	
	# characters are 32 bits wide
	
	# character operators:  increment (++), decrement (--), uppercase ($), lowercase (@)
	
	
	
	

# integer definitions

	# int-08, int-16, int-32, int-64
	
	# unsigned option
	
	# scalar operations:  addition, subtraction, multiplication (⋅ .), division (÷ /), division with rounding (//), remainder (remainder(a ÷ b)), exponentiation (^), increment (++), decrement (--), bitwise and (AND), bitwise or (OR), bitwise xor (XOR), bitwise not (NOT), left shift (<<), right shift (>>)
	
		# use euclidean division for modulus (i.e. the modulus is always positive)
		
		# rounded division:  round up when modulus > even, or when modules = even and the result would otherwise be odd
		
		
		
	
# float declarations

	# float-08, float-16, float-32, float-64
	
	# unsigned option
	
	# scalar operations:  addition, subtraction, multiplication (⋅ .), division (÷ /), exponentiation (^), increment (++), decrement (--)
			
			
			
			
	
# complex numbers

	# represented as two-cell signed floating-point arrays
	
	# scalar operations:  addition, subtraction, multiplication (⋅ .), division (÷ /), exponentiation (^), increment (++), decrement (--), complex conjugate:  *
	
	